from virtstrap.testing import fake_command

FakeInitCommand = fake_command('init')
FakeInstallCommand = fake_command('install')
FakeCleanCommand = fake_command('clean')
FakeInfoCommand = fake_command('info')
